# Dep-test archive telemetry sample

This directory exists only so the dep-test admin backend has a bounded,
read-only filesystem mount to inspect with `statfs`. It contains no waveform
archive data and is not a production archive path.
